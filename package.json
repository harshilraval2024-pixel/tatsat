{
  "name": "tatsat-nrgs",
  "private": true,
  "description": "Tatsat NRGS — website (Next.js) and solar estimator API (FastAPI).",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "refine-logo": "node frontend/scripts/refine-logo.mjs"
  }
}
